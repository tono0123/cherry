<%= form_for(@add_prefid_to_user) do |f| %>
  <% if @add_prefid_to_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@add_prefid_to_user.errors.count, "error") %> prohibited this add_prefid_to_user from being saved:</h2>

      <ul>
      <% @add_prefid_to_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :pref_id %><br>
    <%= f.number_field :pref_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
